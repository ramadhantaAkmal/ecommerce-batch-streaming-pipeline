name: 'ecommerce_batch_stream_pipeline'
version: '1.0.0'
config-version: 2

# Profile configuration
profile: 'ecommerce_batch_stream_pipeline'

# Model paths for Bronze, Silver, and Gold layers
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

# Target directory for compiled SQL
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# Directory for logs
log-path: "logs"

# Source and schema configurations
models:
  ecommerce_batch_stream_pipeline:
    +materialized: table

# Source configuration (referencing external tables)
sources:
  ecommerce_batch_stream_pipeline:
    +schema: akmal_ecommerce_bronze_finpro
    +description: "Source tables from the raw data layer"


